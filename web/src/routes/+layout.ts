// The whole app is client-rendered: it talks to an authenticated API with a
// key held in the browser, so there is nothing a server could usefully render.
export const ssr = false;
export const prerender = false;
